import React from 'react';

import {
  Container
} from './styles';

const Upload: React.FC<Upload> = ({ name, size, type, created_at }) => {
  return (
    <Container />
  );
}

export default Upload;